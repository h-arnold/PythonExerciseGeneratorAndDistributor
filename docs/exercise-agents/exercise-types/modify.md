### Modification Exercise Formats

Modification exercises follow the debugging sets once pupils are confident with the construct in focus. Learners receive working code and must alter it to produce a new, fully specified output. Keep the progression gentle: begin with single-line tweaks, then layer in multi-line or multi-concept edits so the level of difficulty ramps gradually.

#### Requirements checklist

- [ ] **Every code cell must carry exactly one `exerciseN` tag** (case-sensitive: `exercise1`, not `Exercise1` or `exercise_1`). The grading helper resolves cells by tag; missing or misspelled tags will cause tests to fail silently.
- [ ] **Cell metadata must include `"language": "python"`** in the `metadata` object, alongside `"tags": ["exerciseN"]`.
- [ ] **Starter code must be syntactically correct** so students focus on modification, not syntax errors.
- [ ] **Expected output must be shown** in a plain fenced code block inside the markdown instruction cell.
- [ ] **The markdown cell must not reveal the solution or hint at the change mechanism** — describe the *what* (behaviour), not the *how* (implementation).
- [ ] **Scratch or extension cells must be left untagged** so the grader ignores them.
- [ ] **Solution notebooks must not use compact one-liners** — prefer stepwise, per-line transformations (see example below).

#### Notebook layout generated by the scaffolder

- Every exercise appears as a markdown / code cell pair.
- The markdown cell uses a heading of the form `## Exercise N — Title` and includes three subsections:
  - **Working code** shown inside a fenced `python` block.
  - **Task** describing the required change.
  - **Expected output** showing the exact stdout the tests check. Use plain fenced blocks so learners can copy the target result.
- The code cell directly underneath is the student workspace. It contains the unmodified working code and any guiding comments (for example, `# Exercise N — YOUR CODE`). There is **no separate execution cell** for the working solution; students edit this tagged cell in place.
- In the solutions notebook, prefer stepwise, "slow" implementations that perform one small change per line so learners can trace variable state and see how each transformation affects the program. For example:

```python
age = input()
age = int(age)
age = age + 1
print("Next year you will be " + str(age))
```

Avoid compressing multiple operations into a single expression in solution examples, as that hides intermediate steps and reduces learning value.

> **Input and casting convention:** When an exercise requires reading user input and converting it to a numeric type, always split the operation across two lines with clear variable names (e.g., `age_input = input(...)` then `age = int(age_input)`). This makes each step explicit for students: `input()` returns a string, and casting to `int` or `float` is a separate, deliberate operation. Avoid the inline form `age = int(input(...))`, which conflates two concepts in a single expression and can confuse novice learners.

The snippet below reflects the structure emitted by the current scaffolding commands:

```json
{
  "cells": [
    {
      "cell_type": "markdown",
      "metadata": { "language": "markdown" },
      "source": [
        "## Exercise 1 — Change the greeting",
        "",
        "**Working code:**",
        "```python",
        "print(\"Hello World!\")",
        "```",
        "",
        "**Task:** Modify the code so it prints `Hello Python!` instead.",
        "",
        "**Expected output:**",
        "```",
        "Hello Python!",
        "```",
        ""
      ]
    },
    {
      "cell_type": "code",
      "metadata": {
        "language": "python",
        "tags": ["exercise1"]
      },
      "source": [
        "# Exercise 1 — YOUR CODE",
        "# Modify the print statement below",
        "print(\"Hello World!\")"
      ]
    }
  ]
}
```

Tests such as [exercises/sequence/ex002_sequence_modify_basics/tests/test_ex002_sequence_modify_basics.py](../../exercises/sequence/ex002_sequence_modify_basics/tests/test_ex002_sequence_modify_basics.py) call each tagged cell and compare its stdout against the expected output shown in the markdown instructions. When adding new modification exercises, mirror this pattern so both the scaffolding CLI and the grading suite continue to work without adjustment.
